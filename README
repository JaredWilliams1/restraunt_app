This is a mock restraunt order tracking app. In order to run this you need to add a .env file with your MongoDB login information in the following format:

MONGO_DB_USERNAME = YOUR_USERNAME
MONGO_DB_PASSWORD = YOUR_PASSWORD
MONGO_DB_NAME = "database_name"
MONGO_COLLECTION = "collection_name"

The requirements file is included and can be used by package managers like npm to install everything else that is needed